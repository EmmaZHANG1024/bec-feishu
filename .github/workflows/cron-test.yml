name: Cron Test

on:
  schedule:
    # 临时测试：03:00 UTC = 11:00 北京时间（公开仓库定时验证）
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check time
        run: echo "cron test fired at $(date -u)"
